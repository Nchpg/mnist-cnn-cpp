#pragma once

#include <cstddef>

constexpr size_t IMG_WIDTH = 28;
constexpr size_t IMG_HEIGHT = 28;
constexpr size_t PIXELS = IMG_WIDTH * IMG_HEIGHT;
constexpr size_t NUM_CLASSES = 10;
constexpr float NORMALIZE_DIVISOR = 255.0f;